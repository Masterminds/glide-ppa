#!/bin/bash
clear

# source env
. bin/env

for dist in $DISTS; do
	dput ppa:masterminds/glide "$dist/glide_${VERSION_NUMBER}_source.changes"
done

git add .
git commit -m "Release ${VERSION_NUMBER}"
git push